@model DateTime?
@Html.TextBox("", (Model.HasValue ? Model.Value.ToString("yyyy-MM-dd HH:mm") : string.Empty), new
{
    @class = "form-control combodate",
    data_format = "YYYY-MM-DD HH:mm",
    data_template = "YYYY 年 MM 月 DD 日<br/>HH 时 mm 分"
})